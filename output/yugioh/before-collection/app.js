'use strict';
const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const normalize=s=>String(s??'').normalize('NFKD').replace(/\p{M}/gu,'').toLowerCase();
const money=x=>x==null?'Kein zugeordneter Preis':x.toLocaleString('de-DE',{style:'currency',currency:'EUR'});
const storageKey='yugioh-sammler-v1';let exactSets=new Set();let data,prints=[],rows=[],page=0,favorites=new Set(),ownPrices={},detailCard=null,observer;const pageSize=24;
function status(t){$('status').textContent=t;}
function save(){try{localStorage.setItem(storageKey,JSON.stringify({favorites:[...favorites],prices:ownPrices}));}catch{status('Lokales Speichern nicht möglich. Bitte eine Sicherung herunterladen.');}}
function price(p){const mode=$('priceMode').value;if(mode==='manual')return Object.hasOwn(ownPrices,p.id)?ownPrices[p.id]:null;if(mode==='generic')return data.cards[p.card].genericEur;return p.price;}
function priceInRange(value,min,max,unknown){if(unknown)return value==null;if(min===''&&max==='')return true;return value!=null&&(min===''||value>=Number(min))&&(max===''||value<=Number(max));}
function matches(p){const c=data.cards[p.card],q=normalize($('search').value.trim()).replace(/-de(?=\d)/g,'-en'),s=normalize($('set').value.trim()),type=$('type').value;const category=c.frame==='spell'?'spell':c.frame==='trap'?'trap':c.frame==='skill'?'skill':c.frame==='token'?'token':'monster';return(!q||p.search.includes(q))&&(!s||(exactSets.has(s)?normalize(p.set)===s:normalize(p.set).includes(s)||normalize(p.code).split('-')[0]===s))&&(!$('rarity').value||p.rarity===$('rarity').value)&&(!type||category===type)&&(!$('favorites').checked||favorites.has(p.id))&&priceInRange(price(p),$('min').value,$('max').value,$('unknown').checked);}
function cardName(c){return $('language').value==='de'?(c.de||c.name):c.name;}
function makeRows(list){if($('view').value==='prints')return list.map(p=>({card:p.card,prints:[p],representative:p}));const grouped=new Map();for(const p of list){if(!grouped.has(p.card))grouped.set(p.card,{card:p.card,prints:[],representative:p});const row=grouped.get(p.card);row.prints.push(p);const a=price(p),b=price(row.representative);if(a!=null&&(b==null||a<b))row.representative=p;}return [...grouped.values()];}
function render(){if(!data)return;const min=$('min').value,max=$('max').value;const invalid=(min!==''&&Number(min)<0)||(max!==''&&Number(max)<0)||(min!==''&&max!==''&&Number(min)>Number(max));if(invalid)status('Bitte einen gültigen Preisbereich eingeben: Von darf nicht höher als Bis sein.');else if($('status').textContent.startsWith('Bitte einen gültigen'))status('');const filtered=invalid?[]:prints.filter(matches);rows=makeRows(filtered);const sort=$('sort').value;rows.sort((a,b)=>{if(sort==='new')return String(b.representative.date||'').localeCompare(a.representative.date||'')||cardName(data.cards[a.card]).localeCompare(cardName(data.cards[b.card]),'de');if(sort==='asc'||sort==='desc'){const x=price(a.representative),y=price(b.representative);return x==null?(y==null?0:1):y==null?-1:sort==='asc'?x-y:y-x;}return cardName(data.cards[a.card]).localeCompare(cardName(data.cards[b.card]),'de');});const count=Math.max(1,Math.ceil(rows.length/pageSize));page=Math.min(page,count-1);$('results').textContent=rows.length.toLocaleString('de-DE')+($('view').value==='cards'?' Karten':' Druckvarianten')+' · '+filtered.length.toLocaleString('de-DE')+' passende Einträge';$('favoriteCount').textContent=favorites.size+' Favoriten';$('empty').hidden=rows.length>0;$('page').textContent=`Seite ${page+1} von ${count}`;$('prev').disabled=page===0;$('next').disabled=page===count-1;
$('priceNotice').textContent=$('priceMode').value==='generic'?'Karte ab: Allgemeiner Einstiegspreis über mehrere Drucke. Kein Preis der dargestellten Seltenheit oder Ausgabe.':$('priceMode').value==='manual'?'Der Preisfilter verwendet deine selbst eingetragenen Preise. Fehlende Einträge bleiben ohne Preis.':'EUR-Setpreise: Zuordnung über eindeutig übereinstimmende Set-Inhalte. Auflage, Zustand und Sprache sind nicht getrennt bepreist.';
$('grid').innerHTML=rows.slice(page*pageSize,(page+1)*pageSize).map(row=>{const c=data.cards[row.card],p=row.representative,v=price(p),grouped=$('view').value==='cards',saved=row.prints.some(p=>favorites.has(p.id));return `<article class="card"><button class="art" data-detail="${c.id}" aria-label="${esc(cardName(c))}: Details öffnen"><span>Bild wird lokal gespeichert …</span>${c.images[0]?`<img data-image="${c.images[0]}" alt="${esc(c.name)} · englisches Beispielmotiv">`:''}</button><div class="card-body"><div class="card-name">${esc(cardName(c))}</div><div class="english">${esc($('language').value==='de'&&c.de?c.name:c.type)}</div><div class="badges"><span class="badge">${esc(c.type)}</span>${grouped?`<span class="badge gold">${row.prints.length} Varianten</span>`:`<span class="badge gold">${esc(p.rarity)}</span>`}</div><div class="setline">${grouped?esc(p.set)+(row.prints.length>1?' · weitere Sets in Details':''):esc(p.code)+'<br>'+esc(p.set||'Ohne Setzuordnung')}</div><div class="price ${v==null?'missing':''}">${v==null?'Preis nicht verfügbar':money(v)}</div><div class="price-label">${$('priceMode').value==='generic'?'Karte ab · alle Drucke':$('priceMode').value==='manual'?'Eigener Preis':grouped?'Günstigste zugeordnete Variante':'EUR-Trend · Setzuordnung abgeleitet'}</div><div class="card-actions"><button data-detail="${c.id}">${grouped?'Varianten ansehen':'Details'}</button>${grouped?`<button class="fav ${saved?'active':''}" data-detail="${c.id}">${saved?'★ Favoriten':'☆ Favorit wählen'}</button>`:`<button class="fav ${saved?'active':''}" data-fav="${esc(p.id)}" aria-pressed="${saved}">${saved?'★ Favorit':'☆ Favorit'}</button>`}</div></div></article>`;}).join('');loadImages();}
function loadImages(){observer?.disconnect();observer=new IntersectionObserver(entries=>{for(const e of entries){if(!e.isIntersecting)continue;const img=e.target.querySelector('img[data-image]');if(!img)continue;img.src='/images/'+img.dataset.image+'.jpg';img.onload=()=>img.previousElementSibling?.remove();img.onerror=()=>{img.removeAttribute('src');if(img.previousElementSibling)img.previousElementSibling.textContent='Bild aktuell nicht verfügbar. Beim nächsten Öffnen erneut versuchen.';};observer.unobserve(e.target);}},{rootMargin:'100px'});document.querySelectorAll('img[data-image]').forEach(img=>observer.observe(img.parentElement));}
function toggleFavorite(id){if(!prints.some(p=>p.id===id))return;favorites.has(id)?favorites.delete(id):favorites.add(id);save();render();if($('detail').open)detail(detailCard,false);}
function market(p,c){if(p.product)return 'https://www.cardmarket.com/de/YuGiOh/Products?idProduct='+p.product;return 'https://www.cardmarket.com/de/YuGiOh/Products/Search?searchString='+encodeURIComponent(c.name+' '+p.code);}
function detail(id,open=true){detailCard=id;const c=data.cards.find(c=>c.id===id);if(!c)return;const cardIndex=data.cards.indexOf(c);const list=prints.filter(p=>p.card===cardIndex).sort((a,b)=>Number(matches(b))-Number(matches(a))||String(b.date).localeCompare(a.date)||a.code.localeCompare(b.code));$('detailBody').innerHTML=`<div class="detail-top"><div>${c.images[0]?`<img class="detail-image" id="detailImage" src="/images/${c.images[0]}-full.jpg" alt="${esc(c.name)} · englisches Beispielmotiv">`:''}${c.images.length>1?`<label>Alternatives Motiv<select id="artSelect" class="art-select">${c.images.map((id,i)=>`<option value="${id}">Motiv ${i+1}</option>`).join('')}</select></label>`:''}<p class="detail-info">Beispielmotiv · Setnummer und Foil-Ausführung können vom gewählten Druck abweichen.</p></div><div><p class="eyebrow">${esc(c.type)} · ${esc(c.attribute||c.race)}</p><h2>${esc(cardName(c))}</h2><p class="muted">${esc(c.name)} · ID ${c.id}</p><p class="detail-info">${c.atk!=null?'ATK '+c.atk+' · ':''}${c.def!=null?'DEF '+c.def+' · ':''}${c.level!=null?'Level / Rang / Link '+c.level:''}</p><p class="detail-text">${esc($('language').value==='de'?(c.descDe||c.desc):c.desc)}</p><a href="${esc(c.source)}" target="_blank" rel="noopener">Kartenquelle ↗</a>${c.konami?` · <a href="https://www.db.yugioh-card.com/yugiohdb/card_search.action?ope=2&cid=${c.konami}&request_locale=de" target="_blank" rel="noopener">Konami-Datenbank ↗</a>`:''}</div></div><h3>${list.length} Druckvarianten</h3><p class="muted">Passende Filterergebnisse stehen zuerst. Favoriten und eigene Preise gelten jeweils für Set-Code und Seltenheit.</p><div class="table-wrap"><table><thead><tr><th>Set / Nummer</th><th>Seltenheit</th><th>EUR-Trend</th><th>Eigener Preis (€)</th><th>Favorit / Markt</th></tr></thead><tbody>${list.map(p=>`<tr><td>${esc(p.set||'Ohne Setzuordnung')}<br><strong>${esc(p.code)}</strong>${matches(p)?'<br><span class="badge gold">Passt zum Filter</span>':''}</td><td>${esc(p.rarity)}${p.sourceNote?'<br><span class="detail-info">'+esc(p.sourceNote)+'</span>':''}</td><td>${p.price==null?'Nicht zugeordnet':money(p.price)+'<br><span class="detail-info">Zuordnung abgeleitet</span>'}</td><td><input type="number" min="0" step="0.01" data-own="${esc(p.id)}" value="${Object.hasOwn(ownPrices,p.id)?ownPrices[p.id]:''}" aria-label="Eigener Preis für ${esc(p.code+' '+p.rarity)}" placeholder="Offen"><button data-clear="${esc(p.id)}" aria-label="Eigenen Preis löschen: ${esc(p.code+' '+p.rarity)}">×</button></td><td><button class="fav ${favorites.has(p.id)?'active':''}" data-fav="${esc(p.id)}" aria-pressed="${favorites.has(p.id)}">${favorites.has(p.id)?'★':'☆'}</button> <a href="${esc(market(p,c))}" target="_blank" rel="noopener">${p.product?'Cardmarket':'CM-Suche'} ↗</a></td></tr>`).join('')}</tbody></table></div>`;if(open)$('detail').showModal();}
function sets(){const q=normalize($('setsSearch').value);$('setsGrid').innerHTML=data.sets.filter(s=>normalize(s.name+' '+s.code).includes(q)).map(s=>`<button class="set-button" data-set="${esc(s.name)}">${esc(s.name)}<span>${esc(s.code)} · ${s.date||'Datum offen'} · ${s.prints?s.prints+' Druckvarianten':'noch ohne Kartendaten'}</span></button>`).join('');}
document.addEventListener('click',e=>{const clear=e.target.closest('[data-clear]');if(clear){delete ownPrices[clear.dataset.clear];save();render();if($('detail').open)detail(detailCard,false);return;}const f=e.target.closest('[data-fav]');if(f)toggleFavorite(f.dataset.fav);const d=e.target.closest('[data-detail]');if(d)detail(Number(d.dataset.detail));const s=e.target.closest('[data-set]');if(s){$('set').value=s.dataset.set;$('setsDialog').close();page=0;render();}});
function saveOwnPrice(e){const id=e.target.dataset.own;if(!id)return;const value=e.target.value;if(value===''){delete ownPrices[id];}else{const number=Number(value);if(!Number.isFinite(number)||number<0)return;ownPrices[id]=number;}save();render();}
$('detailBody').addEventListener('input',saveOwnPrice);
$('detailBody').addEventListener('change',e=>{if(e.target.id==='artSelect'){$('detailImage').src='/images/'+e.target.value+'-full.jpg';}if(e.target.dataset.own){const id=e.target.dataset.own,v=e.target.value;if(v==='')delete ownPrices[id];else if(Number.isFinite(Number(v))&&Number(v)>=0)ownPrices[id]=Number(v);else{status('Bitte einen gültigen eigenen Preis eingeben.');return;}save();render();}});
for(const id of ['rarity','type','view','sort','language','priceMode','unknown','favorites'])$(id).addEventListener('change',()=>{page=0;render()});for(const id of ['search','set','min','max'])$(id).addEventListener('input',()=>{if(id==='min'||id==='max'){$('budget').value='';$('unknown').checked=false;}page=0;render();});$('budget').onchange=()=>{$('min').value='';$('max').value=$('budget').value;$('unknown').checked=false;page=0;render();};
$('prev').onclick=()=>{page--;render();$('results').scrollIntoView({block:'start'})};$('next').onclick=()=>{page++;render();$('results').scrollIntoView({block:'start'})};$('reset').onclick=()=>{for(const id of ['search','set','min','max','budget','rarity','type'])$(id).value='';$('unknown').checked=false;$('favorites').checked=false;$('sort').value='name';$('priceMode').value='set';page=0;status('');render();};
$('closeDetail').onclick=()=>$('detail').close();$('closeSets').onclick=()=>$('setsDialog').close();$('browseSets').onclick=()=>{sets();$('setsDialog').showModal()};$('setsSearch').oninput=sets;
$('backup').onclick=()=>{const content={format:'yugioh-sammler',version:1,date:new Date().toISOString(),favorites:[...favorites],prices:ownPrices};const url=URL.createObjectURL(new Blob([JSON.stringify(content,null,2)],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='yugioh-sammler-sicherung.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);status('Sicherung mit Favoriten und eigenen Preisen erstellt.');};$('restore').onclick=()=>$('upload').click();$('upload').onchange=async()=>{try{const file=$('upload').files[0];if(!file)return;if(file.size>10000000)throw Error('Datei zu groß');const v=JSON.parse(await file.text());if(v.format!=='yugioh-sammler'||v.version!==1||!Array.isArray(v.favorites)||!v.favorites.every(x=>typeof x==='string')||!v.prices||Array.isArray(v.prices)||typeof v.prices!=='object'||!Object.values(v.prices).every(x=>typeof x==='number'&&Number.isFinite(x)&&x>=0))throw Error('Ungültiges Format');const valid=new Set(prints.map(p=>p.id));let n=0;for(const id of v.favorites)if(valid.has(id)){favorites.add(id);n++;}for(const [id,x]of Object.entries(v.prices))if(valid.has(id))ownPrices[id]=x;save();page=0;render();status(n+' gültige Favoriten und eigene Preise übernommen.');}catch(e){status('Sicherung konnte nicht geladen werden: '+e.message);}finally{$('upload').value=''}};
(async()=>{if(location.protocol==='file:'){status('Bitte den Browser über Start.cmd im selben Ordner öffnen.');$('results').textContent='Start.cmd öffnen';return;}try{const r=await fetch('/catalog.json');if(!r.ok)throw Error('Katalog nicht erreichbar');data=await r.json();exactSets=new Set(data.sets.map(s=>normalize(s.name)));const dateMap=new Map(data.sets.map(s=>[s.name,s.date]));const indexes=new Set(data.prints.map(p=>p.card));prints=[...data.prints];for(let i=0;i<data.cards.length;i++)if(!indexes.has(i))prints.push({id:data.cards[i].id+'|unassigned',card:i,set:'',code:String(data.cards[i].id),rarity:'Ohne Setzuordnung',price:null,product:null});for(const p of prints){const c=data.cards[p.card];p.date=dateMap.get(p.set)||'';p.search=normalize([c.name,c.de,c.id,c.archetype,p.set,p.code,p.rarity].join(' '));}try{const saved=JSON.parse(localStorage.getItem(storageKey)||'{}');const ids=new Set(prints.map(p=>p.id));favorites=new Set((Array.isArray(saved.favorites)?saved.favorites:[]).filter(id=>ids.has(id)));ownPrices=Object.fromEntries(Object.entries(saved.prices||{}).filter(([id,v])=>ids.has(id)&&typeof v==='number'&&Number.isFinite(v)&&v>=0));}catch{status('Gespeicherte Daten konnten nicht gelesen werden. Du kannst eine Sicherung laden.');}
$('setlist').innerHTML=data.sets.map(s=>`<option value="${esc(s.name)}">${esc(s.code)} · ${s.prints} Varianten</option>`).join('');$('rarity').innerHTML='<option value="">Alle Seltenheiten</option>'+[...new Set(prints.map(p=>p.rarity))].sort().map(s=>`<option>${esc(s)}</option>`).join('');$('budget').innerHTML='<option value="">Alle Preise</option>'+Array.from({length:20},(_,i)=>`<option value="${(i+1)*5}">Bis ${(i+1)*5} €</option>`).join('');$('totals').innerHTML=[[data.meta.cardCount,'Karten'],[data.meta.setCount,'Sets'],[data.meta.printCount,'Druckvarianten']].map(([n,l])=>`<div><strong>${n.toLocaleString('de-DE')}</strong><span>${l}</span></div>`).join('');render();}catch(e){status('Der Katalog konnte nicht geladen werden. Bitte Start.cmd erneut öffnen. '+e.message);$('results').textContent='Katalog nicht verfügbar';}})();
