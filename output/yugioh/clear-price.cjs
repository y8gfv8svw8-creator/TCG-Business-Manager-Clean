const fs=require('fs');const p=__dirname+'/YuGiOh-Sammler/app.js';let s=fs.readFileSync(p,'utf8');s=s.replace('placeholder="Offen"></td>','placeholder="Offen"><button data-clear="${esc(p.id)}" aria-label="Eigenen Preis löschen: ${esc(p.code+\' \'+p.rarity)}">×</button></td>');s=s.replace("document.addEventListener('click',e=>{", "document.addEventListener('click',e=>{const clear=e.target.closest('[data-clear]');if(clear){delete ownPrices[clear.dataset.clear];save();render();if($('detail').open)detail(detailCard,false);return;}");fs.writeFileSync(p,s);
