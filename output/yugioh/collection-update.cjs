const fs=require('fs'),path=require('path');const src='C:/Users/stefa/Desktop/YuGiOh-Sammler',out=path.join(__dirname,'YuGiOh-Sammler');for(const n of ['app.js','index.html','style.css'])fs.copyFileSync(path.join(src,n),path.join(out,n));
let s=fs.readFileSync(out+'/app.js','utf8'),h=fs.readFileSync(out+'/index.html','utf8');
function change(a,b){if(!s.includes(a))throw Error('Fehlende Fundstelle '+a.slice(0,80));s=s.replace(a,b);}
change('let exactSets=new Set();','let owned=new Set(),detailSet=null;const selections=new Map();let exactSets=new Set();');
change('prices:ownPrices}));','prices:ownPrices,owned:[...owned]}));');
change('function matches(p){','function matches(p,ignoreOwnership=false){');
change("return(!q||p.search.includes(q))", "return(ignoreOwnership||$('ownership').value!=='owned'&&$('ownership').value!=='missing'||($('ownership').value==='owned'?owned.has(p.id):!owned.has(p.id)))&&(!q||p.search.includes(q))");
const start=s.indexOf('function makeRows('),end=s.indexOf('function render()',start);
s=s.slice(0,start)+`function compareSetNumber(a,b){return a.code.localeCompare(b.code,'en',{numeric:true,sensitivity:'base'})||a.set.localeCompare(b.set)||a.rarity.localeCompare(b.rarity);}
function makeRows(list){if($('view').value==='prints')return list.map(p=>({key:p.id,card:p.card,prints:[p],representative:p}));const grouped=new Map();for(const p of list){const key=JSON.stringify([p.card,p.set]);if(!grouped.has(key))grouped.set(key,{key,card:p.card,prints:[],representative:p});grouped.get(key).prints.push(p);}for(const row of grouped.values()){row.prints.sort(compareSetNumber);row.representative=row.prints.find(p=>p.id===selections.get(row.key))||row.prints[0];}return [...grouped.values()];}
function ownershipControl(p){return '<label class="ownership-check"><input type="checkbox" data-owned="'+esc(p.id)+'" '+(owned.has(p.id)?'checked':'')+' aria-label="Im Besitz: '+esc(p.code+' '+p.rarity)+'"> '+(owned.has(p.id)?'Im Besitz':'Fehlt')+'</label>';}
function variantPicker(row){return '<select data-variant="'+esc(row.key)+'" aria-label="Variante für '+esc(row.representative.code)+'">'+row.prints.map(p=>'<option value="'+esc(p.id)+'" '+(p.id===row.representative.id?'selected':'')+'>'+esc(p.code+' · '+p.rarity)+(owned.has(p.id)?' ✓':'')+'</option>').join('')+'</select>';}
function collectionList(visible){return '<div class="table-wrap"><table class="collection-table"><thead><tr><th>Besitz</th><th>Setnummer / Karte</th><th>Set</th><th>Variante in diesem Set</th><th>Preis</th><th>Link / Details</th></tr></thead><tbody>'+visible.map(row=>{const p=row.representative,c=data.cards[row.card];return '<tr class="'+(owned.has(p.id)?'owned-row':'')+'"><td>'+ownershipControl(p)+'</td><td><strong>'+esc(p.code)+'</strong><br>'+esc(cardName(c))+'</td><td>'+esc(p.set||'Ohne Setzuordnung')+'</td><td>'+variantPicker(row)+'</td><td>'+money(price(p))+'</td><td><a href="'+esc(market(p,c))+'" target="_blank" rel="noopener">'+(p.product?'Cardmarket':'CM-Suche')+' ↗</a><br><button data-detail="'+c.id+'" data-scope="'+esc(p.set)+'">Set-Details</button></td></tr>';}).join('')+'</tbody></table></div>';}
`+s.slice(end);
change("rows.sort((a,b)=>{if(sort==='new')", "rows.sort((a,b)=>{if(sort==='setNumber')return compareSetNumber(a.representative,b.representative);if(sort==='new')");
change("$('favoriteCount').textContent=favorites.size+' Favoriten';", "$('favoriteCount').textContent=favorites.size+' Favoriten';const scope=prints.filter(p=>matches(p,true)),have=scope.filter(p=>owned.has(p.id)).length;$('collectionCount').textContent=have.toLocaleString('de-DE')+' von '+scope.length.toLocaleString('de-DE')+' Varianten im Besitz · '+(scope.length-have).toLocaleString('de-DE')+' fehlen';");
change("$('grid').innerHTML=rows.slice", "$('grid').className=$('view').value==='list'?'collection-list':'grid';$('grid').innerHTML=$('view').value==='list'?collectionList(rows.slice(page*pageSize,(page+1)*pageSize)):rows.slice");
change("grouped=$('view').value==='cards'", "grouped=$('view').value!=='prints'");
s=s.replaceAll('data-detail="${c.id}"','data-detail="${c.id}" data-scope="${esc(p.set)}"');
change("esc(p.set)+(row.prints.length>1?' · weitere Sets in Details':'')", "esc(p.code)+'<br>'+esc(p.set)");
change('<div class="card-actions"><button data-detail=', '${ownershipControl(p)}${grouped?variantPicker(row):\'\'}<a class="print-link" href="${esc(market(p,c))}" target="_blank" rel="noopener">${p.product?\'Cardmarket\':\'CM-Suche · Set & Variante\'} ↗</a><div class="card-actions"><button data-detail=');
change("'Günstigste zugeordnete Variante'", "'Ausgewählte Variante · EUR-Trend'");
change("encodeURIComponent(c.name+' '+p.code)","encodeURIComponent(c.name+' '+p.code+' '+p.set+' '+p.rarity)");
change('function detail(id,open=true){detailCard=id;', 'function detail(id,open=true,setName=detailSet){detailSet=setName;detailCard=id;');
change('prints.filter(p=>p.card===cardIndex)', 'prints.filter(p=>p.card===cardIndex&&(detailSet==null||p.set===detailSet))');
change('<h3>${list.length} Druckvarianten</h3>', '<h3>${esc(detailSet||\'Ohne Setzuordnung\')} · ${list.length} Druckvarianten</h3>');
change('Passende Filterergebnisse stehen zuerst. Favoriten und eigene Preise gelten jeweils für Set-Code und Seltenheit.', 'Nur Varianten aus diesem Set. Besitz, Favoriten und eigene Preise gelten jeweils für Set-Code und Seltenheit.');
change('<th>Favorit / Markt</th>', '<th>Besitz / Favorit / Markt</th>');
change('<td><button class="fav ${favorites.has(p.id)', '<td>${ownershipControl(p)}<button class="fav ${favorites.has(p.id)');
change('if(d)detail(Number(d.dataset.detail));',"if(d)detail(Number(d.dataset.detail),true,d.dataset.scope??null);");
change("'priceMode','unknown','favorites']", "'priceMode','unknown','favorites','ownership']");
change("$('unknown').checked=false;$('favorites').checked=false;", "$('unknown').checked=false;$('favorites').checked=false;$('ownership').value='';");
change("version:1,date:new Date().toISOString(),favorites:[...favorites],prices:ownPrices", "version:2,date:new Date().toISOString(),favorites:[...favorites],prices:ownPrices,owned:[...owned]");
change("v.version!==1||", "![1,2].includes(v.version)||v.owned!==undefined&&(!Array.isArray(v.owned)||!v.owned.every(x=>typeof x==='string'))||");
change("let n=0;for(const id of v.favorites)", "for(const id of v.owned||[])if(valid.has(id))owned.add(id);let n=0;for(const id of v.favorites)");
change("favorites=new Set((Array.isArray(saved.favorites)", "owned=new Set((Array.isArray(saved.owned)?saved.owned:[]).filter(id=>ids.has(id)));favorites=new Set((Array.isArray(saved.favorites)");
change("Sicherung mit Favoriten und eigenen Preisen erstellt.","Sicherung mit Besitzliste, Favoriten und eigenen Preisen erstellt.");
change("gültige Favoriten und eigene Preise übernommen.","gültige Favoriten sowie Besitzliste und eigene Preise übernommen.");
s+=`\ndocument.addEventListener('change',e=>{if(e.target.dataset.owned){const id=e.target.dataset.owned;if(!prints.some(p=>p.id===id))return;e.target.checked?owned.add(id):owned.delete(id);save();render();if($('detail').open)detail(detailCard,false);}if(e.target.dataset.variant){const key=e.target.dataset.variant,id=e.target.value;const row=rows.find(r=>r.key===key);if(row&&row.prints.some(p=>p.id===id)){selections.set(key,id);render();}}});\n`;
h=h.replace('<select id="view">','<select id="view"><option value="list">Besitzliste je Set</option>').replace('Karten zusammenfassen','Kartenansicht je Set').replace('<option value="new">','<option value="setNumber">Setnummer aufsteigend</option><option value="new">').replace('<label>Bild / Namen','<label>Besitz<select id="ownership"><option value="">Alle Karten</option><option value="owned">Nur im Besitz</option><option value="missing">Nur fehlende Karten</option></select></label><label>Bild / Namen').replace('<div id="grid"','<p id="collectionCount" class="collection-count" aria-live="polite"></p><div id="grid"').replace('Favoriten sichern','Sammlung sichern').replace('Favoriten und eigene Preise werden','Besitzliste, Favoriten und eigene Preise werden').replace('Favoriten je Druckvariante','Besitzliste und Favoriten je Druckvariante');
h=h.replace('<div class="notice" id="priceNotice">','<p class="collection-help">Hake die ausgewählte Druckvariante ab. Die Variantenauswahl und die Details enthalten nur Drucke aus dem jeweiligen Set. Ein Häkchen gilt nicht automatisch für andere Seltenheiten oder Sets. „CM-Suche“ wird angezeigt, wenn kein eindeutiger Produktlink vorliegt.</p><div class="notice" id="priceNotice">');
fs.writeFileSync(out+'/app.js',s);fs.writeFileSync(out+'/index.html',h);fs.appendFileSync(out+'/style.css',`\n.collection-count{color:var(--gold);font-size:14px}.collection-help{font-size:12px;color:var(--muted);margin:16px 0}.collection-list{background:var(--panel);border:1px solid var(--border);border-radius:12px;overflow:hidden}.collection-table td{vertical-align:middle}.collection-table select{max-width:300px;min-width:170px}.collection-table td:nth-child(2){min-width:190px}.collection-table td:nth-child(3){min-width:160px}.ownership-check{display:flex;flex-direction:row;align-items:center;gap:8px;white-space:nowrap;padding:8px 0;color:var(--text)}.ownership-check input{min-height:0;accent-color:var(--gold);width:18px;height:18px}.owned-row{background:#28312366}.card-body select{width:100%;margin:6px 0}.print-link{display:block;font-size:12px;margin:8px 0}.collection-table button{margin-top:5px}\n`);
console.log('Besitzliste, Sicherung, Filter, Setnummer und setbezogene Varianten ergänzt.');
