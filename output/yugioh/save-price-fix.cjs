const fs=require('fs');const file=__dirname+'/YuGiOh-Sammler/app.js';let s=fs.readFileSync(file,'utf8');const anchor="$('detailBody').addEventListener('change',";if(!s.includes("addEventListener('input',saveOwnPrice)")){const insert="function saveOwnPrice(e){const id=e.target.dataset.own;if(!id)return;const value=e.target.value;if(value===''){delete ownPrices[id];}else{const number=Number(value);if(!Number.isFinite(number)||number<0)return;ownPrices[id]=number;}save();render();}\n$('detailBody').addEventListener('input',saveOwnPrice);\n";s=s.replace(anchor,insert+anchor);}fs.writeFileSync(file,s);
